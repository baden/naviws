%%-*- mode: erlang -*-
{application, naviws, [
    {description, "Websocket application library for ErlNaviCC project"},
    {vsn, "0.0.1"},
    {id, "git"},
    {modules, []},
    {registered, [naviws_sup]},
    {applications, [kernel,stdlib,cowboy,jsxn,navidb]},
    {mod, {naviws_app, []}},
    {env, [
        {ws_listen_port, 8983},
        {acceptors, 100}
    ]},
    {start_phases, [
        {listen, []}
    ]}
]}.
